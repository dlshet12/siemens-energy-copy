# UI Platform — Documentation

Central docs for the design system. Add:

- **Getting started**: install, load tokens, use components in HTML/React/Angular/Vue.
- **Design tokens**: token categories, theming, overriding.
- **Components**: link to Storybook; API (attributes, events, slots).
- **Accessibility**: keyboard, ARIA, contrast.
- **Adapters**: how React/Angular/Vue packages wrap core.

Storybook is the primary component preview and usage reference.
