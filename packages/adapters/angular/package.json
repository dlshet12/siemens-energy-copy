{
  "name": "@ui-platform/angular",
  "version": "1.0.0",
  "description": "Angular adapters for @ui-platform/core — CVA or wrapper directives",
  "type": "module",
  "peerDependencies": {
    "@ui-platform/core": "*",
    "@angular/core": ">=17.0.0"
  },
  "scripts": {
    "build": "echo 'Adapter stub — implement custom elements schema or wrapper components'",
    "clean": "rm -rf dist"
  }
}
