{
  "name": "@ui-platform/vue",
  "version": "1.0.0",
  "description": "Vue adapters for @ui-platform/core — defineCustomElement or wrapper components",
  "type": "module",
  "peerDependencies": {
    "@ui-platform/core": "*",
    "vue": ">=3.0.0"
  },
  "scripts": {
    "build": "echo 'Adapter stub — implement Vue custom elements or wrappers'",
    "clean": "rm -rf dist"
  }
}
